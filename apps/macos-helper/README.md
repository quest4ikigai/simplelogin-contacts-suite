# macOS Helper

Placeholder for a future native helper app and optional MailKit extension.

The helper is convenience only. The SMTP proxy remains the enforcement layer and
must work without any macOS-specific integration.
