# Contacts Core

Shared helpers for generated SimpleLogin contact records.

The SMTP proxy does not require contacts sync, but the existing server sync app
and future macOS helper can share generated-contact naming conventions here.
